  # 1. 改完本地代码后，运行：
  cd ~/Desktop/thu-secondhand-agent
  python sync.py

  # 2. 脚本会自动：
  #    - 上传所有 backend/api/*.py + main.py + prompt 到服务器（覆盖）
  #    - 重启服务
  #    - 验证是否正常运行

  同步规则：
  - 覆盖服务器上的同名文件，不会产生多余副本
  - 不会上传 backend/data/ 下的交易数据（items.db 等），服务器上的商品数据保留不变




  在项目目录下运行：

  cd ~/Desktop/thu-secondhand-agent
  python reset_data.py

  它会：
  1. 清空本地 SQLite 数据库（items 表、tag_stats 表）
  2. 清空本地 uploads/ 目录下的图片
  3. SSH 连上服务器，远程执行同样的清空操作
  4. 重启服务器上的后端服务

  运行时会让你输入 YES 确认才执行，不会误操作。